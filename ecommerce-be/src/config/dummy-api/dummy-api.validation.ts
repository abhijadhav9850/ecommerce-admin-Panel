import Joi from "joi";

export const dummy_api_body_schema = Joi.object({
    // phone: Joi.string().min(10).max(10).required()
})

export const dummy_api_query_schema = Joi.object({
    // phone: Joi.string().min(10).max(10).required()
})