import Joi from "joi";

export const employee_list_body_schema = Joi.object({
    // phone: Joi.string().min(10).max(10).required()
})

export const employee_list_query_schema = Joi.object({
    // phone: Joi.string().min(10).max(10).required()
})